# 1. Install Streamlit
pip install streamlit

# 2. Install SQLite CLI 
sudo apt update
sudo apt install sqlite3

# 3. (Optional) Install any other required packages — if your script uses pandas, etc.
pip install pandas numpy

streamlit run WithoutGarbageValue.py

